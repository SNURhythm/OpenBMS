{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "cmake",
            "args": [
                "--build",
                "${workspaceFolder}/cmake-build-debug",
                "--target",
                "main",
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}