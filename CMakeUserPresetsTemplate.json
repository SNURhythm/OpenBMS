{
    "version": 3,
    "configurePresets": [
        {
            "name": "user-debug",
            "inherits": "debug",
            "environment": {
                "VCPKG_ROOT": "${VCPKG_ROOT}"
            }
        },
        {
            "name": "user-release",
            "inherits": "release",
            "environment": {
                "VCPKG_ROOT": "${VCPKG_ROOT}"
            }
        },
        {
            "name": "user-debug-windows",
            "inherits": "debug-windows",
            "environment": {
                "VCPKG_ROOT": "${VCPKG_ROOT}"
            }
        },
        {
            "name": "user-release-windows",
            "inherits": "release-windows",
            "environment": {
                "VCPKG_ROOT": "${VCPKG_ROOT}"
            }
        }
    ]
}